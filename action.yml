name: "Notify Discord"
description: "Sends a notification to a Discord channel"
inputs:
  branch:
    description: "The branch that was pushed to"
    required: true
    default: "master"
runs:
  using: "node16"
  main: "src/index.js"
